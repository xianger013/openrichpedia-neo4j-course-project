# data/processed

运行：

```bash
python scripts/preprocess.py
```

会在本目录生成：

- `nodes.csv`
- `relationships.csv`
- `stats.json`

这些文件属于正式完整数据的本地生成结果，默认由 `.gitignore` 排除。提交课程作业时，是否附带 CSV 请按老师对文件大小和数据许可的要求决定。
